const name = 'Prayash';
const location = 'Los Angeles';

// console.log(`${name} is from ${location}.`);

const addNumbers = (x, y) => {
  return x + y;
};

// exporting name;
// module.exports = name;

//exporting location
// module.exports = location;

//exporting a function addNumbers;
module.exports = addNumbers;
